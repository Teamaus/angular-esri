import { A2Directive } from './a2.directive';

describe('A2Directive', () => {
  it('should create an instance', () => {
    const directive = new A2Directive();
    expect(directive).toBeTruthy();
  });
});
